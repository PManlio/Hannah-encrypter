#include "hannah.h"

int main() {
	// As soon as the new class is created, the constructor is invoked
	Hannah Cript;
	// Now we can call the init method
	Cript.Init();
	// then we can call Encrypt() method
	Cript.Encrypt();
	// control to palindrome words has been added.
}